/**
 * Re-exports the canonical useIsMobile from the shared implementation.
 * Use this hook for mobile detection within feature components.
 * The raw implementation lives at ~/hooks/use-mobile (shadcn-compatible).
 */
export { useIsMobile } from "~/hooks/use-mobile";
